/**
 * SocialForest — canonical local paths
 *
 * All docs and scripts should reference these constants.
 * When the project folder moves on disk, update ONLY this file.
 */

/** Windows local clone / working copy (GitHub Desktop, npm, IDE) */
export const LOCAL_PROJECT_PATH_WINDOWS = 'D:\\sak\\ApplicationDevelopment\\socialForest';

/** POSIX-style path for cross-platform docs */
export const LOCAL_PROJECT_PATH_POSIX = '/d/sak/ApplicationDevelopment/socialForest';

/** Folder name (git repo root) */
export const PROJECT_DIR_NAME = 'socialForest';

/** Parent folder on Windows */
export const LOCAL_PARENT_DIR_WINDOWS = 'D:\\sak\\ApplicationDevelopment';
